# Copy to dev/.env and adjust.

# Uncomment both lines to pass an RS-485 adapter into the gateway container.
# RTU_DEVICE is the host device; prefer the stable /dev/serial/by-id/ path.
#COMPOSE_FILE=compose.yaml:compose.rtu.yaml
#RTU_DEVICE=/dev/serial/by-id/usb-FTDI_USB-RS485-if00-port0
